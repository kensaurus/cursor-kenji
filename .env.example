# Copy to .env (gitignored) — never commit real values.
# Granular npm token with publish access to @kensaurus (Bypass 2FA optional).
NPM_TOKEN=npm_your_token_here

# fal.ai — Key ID:Key Secret. Used only for local asset generation.
FAL_KEY=keyid:keysecret
