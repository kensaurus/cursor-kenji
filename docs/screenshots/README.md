# README capability cards

Theme-aware hero + tour frames used by the root [README](../../README.md).

There is **no hosted app**. Recapture from `src/showcase.html` at 1600×1000 (dark/light via `?theme=` and `?view=`), then promote PNGs here. Do not commit `.playwright-mcp/` scratch.
